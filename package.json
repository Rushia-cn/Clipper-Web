{
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "npm install -g @vue/cli && npm install -g @vue/cli-service-global && vue build index.js",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
    "vue-youtube-embed": "^2.2.2",
    "vue": "*",
    "@vue/cli-service": "~4.4.0"
  },
  "devDependencies": {
    "@vue/cli-service": "~4.4.0"
  }
}
